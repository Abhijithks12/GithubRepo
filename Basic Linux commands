ls: List files and directories in the current directory.

cd: Change directory. For example, cd /path/to/directory will change to the specified directory.

pwd: Print the current working directory.

mkdir: Create a new directory. For example, mkdir new_directory will create a directory named "new_directory" in the current directory.

touch: Create a new file. For example, touch new_file.txt will create a file named "new_file.txt" in the current directory.

rm: Remove files or directories. For example, rm file.txt will delete a file named "file.txt", 
      and rm -r directory will delete a directory and its contents recursively.
cp: Copy files and directories. For example, cp file.txt destination will copy the file "file.txt" to the specified destination.

mv: Move or rename files and directories. For example, mv file.txt new_location will move the file "file.txt" to the specified location,
      and mv old_name.txt new_name.txt will rename the file.

cat: View the contents of a file. For example, cat file.txt will display the contents of "file.txt" in the terminal.

grep: Search for a pattern in a file or output. For example, grep "keyword" file.txt will search for the specified keyword in "file.txt".

chmod: Change permissions of a file or directory. For example, chmod +x script.sh will add executable permissions to the file "script.sh".

sudo: Execute a command with administrative privileges. For example, sudo apt-get update will update the system using the apt package manager.
